version: '3.8'

services:
  web:
    build: .
    command: 'uvicorn main:app --reload'
    ports:
      - 8000:8000
    depends_on:
      - db
  db:
    image: postgres:15-alpine
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
    env_file:
    - .env